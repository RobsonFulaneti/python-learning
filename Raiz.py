import math
n1 = int(input('Digite um numero: '))
raiz = math.sqrt(n1)
print('a raiz de {} é igual a {:.2f}'.format(n1, raiz))

# Biblioteca math, import math importa todas funcionalidades

from math import sqrt
n1 = int(input('Digite um numero: '))
raiz = sqrt(n1)
print('a raiz de {} é igual a {:.2f}'.format(n1, raiz))

# Biblioteca math com from utilizando para importar somente a função que quiser